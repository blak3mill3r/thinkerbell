todo
  singleton cubin loader with a map<std::string, cuda::Program>? mt probs
  multithreaded host code with boost::thread
  struct for each kernel's parameters
  weight update kernels
  use write-combining memory for transferring training examples to the GPU
